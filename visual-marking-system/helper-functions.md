# Helper Functions

Helper functions are available on **AC\_VisualMarkingSystem** under the Helper Functions category.

These can be used to:

- Check if an actor is currently outlined
- Check if an actor already has a symbol attached

They are optional but recommended for clean toggle logic. For example, checking whether a symbol already exists before spawning a new one prevents duplicate symbols being attached to the same actor.
